function calculoTriangulo() {
  const base = prompt("qual valor da base do triangulo?:");
  const altura = prompt("qual valor da altura do triangulo?;");
  return base * altura;
}
resultado = calculoTriangulo();
console.log(resultado);
